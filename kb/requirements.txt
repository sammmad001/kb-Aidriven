# Personal Knowledge Base - Dependencies
fastapi>=0.115.0
uvicorn>=0.34.0
neo4j>=5.0.0
httpx>=0.28.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
pymupdf>=1.24.0
readability-lxml>=0.8
html2text>=2024.2
python-multipart>=0.0.30
pytest>=9.0
pytest-asyncio>=0.24
respx>=0.22
lark-oapi>=1.0.0
apscheduler>=3.10
cryptography>=42.0

# V2.1: Social media content fetching (ECS Playwright)
playwright>=1.48
# playwright-stealth is NOT on PyPI; the code handles its absence gracefully
# browserforge — browser fingerprint generation (no PyPI package; install via git)
# See: https://github.com/kaliiiiiiiiii/browserforge

# V2.1: Free local OCR (PaddleOCR) — installed separately on ECS
# pip install paddlepaddle==3.2.2 paddleocr

# V2.1: OCR fallback — DashScope qwen-vl-max (uses OpenAI-compatible SDK)
openai>=1.0

# Multi-user auth & isolation
python-jose[cryptography]>=3.3
bcrypt>=4.0
aiosqlite>=0.20
