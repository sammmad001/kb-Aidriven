# 个人知识库系统 — 知识推理优化方案 V1.1

> **基于版本**：V1.0（个人知识库系统-V1.0-产品存档文档.md）  
> **实施日期**：2026-06-10  
> **状态**：P0 优化已实施；V1.1 的优化成果已被 **V1.2 查询管道**（EntityResolver + QualityGate + PipelineTrace）和 **V2.0 多用户架构**（DeepSeek 迁移 + JWT 认证）完整继承  
> **验证**：176 测试通过
>
> **⚠️ 模型引用说明（2026-06 更新）**：本文档原文使用 DashScope（qwen3.5-plus / qwen-flash / qwen-turbo）模型名称。V2.0 迁移后，这些模型已被 DeepSeek 等效模型替代（`deepseek-v4-pro` / `deepseek-v4-flash`），DashScope 仅保留 ASR / OCR 用途。下方表格和配置项中已标注等效映射。详见 [DeepSeek 迁移方案文档](DeepSeek迁移方案文档.md)。

---

## 一、优化概述

本次优化针对 V1.0 系统在**分类准确性**、**链接发现能力**、**逻辑推理精度**三个核心维度上的瓶颈，实施了 5 项 P0 级改进。

### 优化前后对比

| 维度 | V1.0（优化前） | V1.1（优化后） |
|------|---------------|---------------|
| 隐式推理模型 | ~~qwen-flash~~ deepseek-v4-flash（中等推理能力） | ~~**qwen3.5-plus**~~ **deepseek-v4-pro**（强推理能力） |
| 分类判定 | 单次 LLM 判断，无验证 | 决策树 + 置信度 + 分类理由 |
| 实体上限 | 固定 10 个 | 动态（5-30，按内容长度） |
| 隐式推理上下文 | 1-hop 邻居（LIMIT 10） | **2-hop 子图 + 桥接节点** |
| 隐式推理 Prompt | Zero-shot 简单描述 | 推理策略 + 反例 + 置信度校准 |
| 推理验证 | 无（fire-and-forget） | 节点存在检查 + 显式边去重 + 置信度过滤 |
| LLM 容错 | 单次调用，失败即报错 | **二级回退链**（deepseek-v4-pro → deepseek-v4-flash，指数退避） |
| Prompt 增强 | 纯 Zero-shot | **3-5 shot 外部示例**（config/prompts/） |
| 内容截断 | 4000 字符 | **8000 字符**（长文档不丢失信息） |

---

## 二、已实施变更清单

### 2.1 模型分配优化

**文件**：`kb/app/config.py`

| 配置项 | V1.0 旧值 | V1.1 值 | V2.0 当前值 | 理由 |
|--------|-----------|---------|-------------|------|
| ~~`dashscope_model_reasoning`~~ → `deepseek_model_reasoning` | ~~`qwen-flash`~~ | ~~`qwen3.5-plus`~~ | **`deepseek-v4-pro`** | 隐式推理是核心差异化能力，使用 DeepSeek 最强推理模型 |

> **V2.0 迁移说明**：配置项从 `dashscope_model_reasoning` 重命名为 `deepseek_model_reasoning`，默认值更新为 `deepseek-v4-pro`。DashScope 模型配置仅保留用于 ASR / OCR 后端。

**文件**：`kb/.env.example` — 新增分环节模型配置说明

### 2.2 素材分类 Prompt 重新设计

**文件**：`kb/app/ingest/analyze.py`

| 变更点 | 详情 |
|--------|------|
| 分类方式 | 从扁平列表 → **4步优先级决策树**（comparative > relational > experiential > conceptual/factual） |
| 实体判定 | 从笼统规则 → **四条件精确判定**（具体性/可定义性/知识性/稳定性） |
| 禁止类型 | 增加趋势描述/修饰性概念/极短词过滤 |
| 上限策略 | 固定10 → **动态上限**（500→5, 2000→10, 8000→20, ≥8000→30） |
| 实体字段 | 新增 `aliases`/`subtype`/`domain`/`definition` 结构化字段 |
| 冲突字段 | 新增 `confidence`/`suggestion` 字段 |
| 输入截断 | 4000字符 → **8000字符** |
| 分类元数据 | 新增 `type_confidence`/`classification_reason` 字段 |

**文件**：`kb/app/models.py` — `AnalysisReport` 新增 `type_confidence` 和 `classification_reason` 字段

### 2.3 隐式关系推理优化

**文件**：`kb/app/ingest/graph_process.py`

| 变更点 | 详情 |
|--------|------|
| 推理 Prompt | 从简单描述 → **推理策略（先排除→按类型推理）+ 5种类型的触发条件+反例+置信度校准指南** |
| 上下文范围 | 1-hop（LIMIT 10）→ **2-hop 子图（LIMIT 50）+ 高 PageRank 桥接节点（TOP 10）** |
| 推理验证 | 无 → **`_validate_implicit_relations`方法**：节点存在检查 + 自引用过滤 + 去重 + 显式边冲突检测 |
| 低置信度过滤 | 无 → 低于 0.3 的隐式关系直接丢弃 |
| 内容编写 | 增加编写原则（数据溯源/wikilinks规范/关系引用） |

### 2.4 Few-shot Learning 引入

**新增文件**：
- `kb/config/prompts/analyze_fewshot.txt` — 3 个分类示例（factual/conceptual/comparative）
- `kb/config/prompts/implicit_fewshot.txt` — 5 个隐式推理示例（每种类型 1 个）
- `kb/app/ingest/fewshot.py` — 外部文件加载器，LRU 缓存，优雅降级

**修改文件**：
- `kb/app/ingest/analyze.py` — 自动注入 `analyze_fewshot.txt`
- `kb/app/ingest/graph_process.py` — 自动注入 `implicit_fewshot.txt`

### 2.5 LLM 回退链

**文件**：`kb/app/llm.py`

| 变更点 | 详情 |
|--------|------|
| 新类 | `ResilientLLMClient` 包装器 |
| 回退链 | ~~qwen3.5-plus → qwen-flash → qwen-turbo~~ → **deepseek-v4-pro → deepseek-v4-flash**（每级 1 次重试 + 指数退避） |
| 工厂方法 | `get_llm_client()` 自动为 DeepSeek / DashScope 客户端包装 ResilientLLMClient |
| Ollama | 不回退（本地单模型） |

> **V2.0 更新**：ResilientLLMClient 新增 `_DEEPSEEK_FALLBACKS` 回退链（`deepseek-v4-pro → deepseek-v4-flash`）。DeepSeek 客户端通过 `DeepSeekClient` 类（OpenAI 兼容 API，30s 超时）直接创建，然后自动包装 ResilientLLMClient。原有的 `_DASHSCOPE_FALLBACKS` 回退链保留，但 DashScope 仅用于 ASR/OCR，不再参与核心推理流程。

---

## 三、未实施项（P1-P2，建议后续迭代）

| 优先级 | 项目 | 说明 |
|--------|------|------|
| P1 | 节点类型细化（14小类） | 需 models.py + database.py schema 变更 + 数据迁移 |
| ✅ 已实施 | ~~边类型扩展（+5种新标签）~~ | ~~CAUSES/PRECEDES/IS_A/CONTRADICTS/ANALOGOUS_TO~~ — **已在 `graph_process.py` 中实施**，5 种隐式关系类型已定义于 `models.py`（ImplicitRelationType）并集成到推理 Prompt 中 |
| P1 | GDS PageRank + Leiden | 需确认 Neo4j 版本 GDS 可用性 |
| P1 | 向量嵌入层 | 需集成 text-embedding-v3 API |
| ✅ 已实施 | ~~实体别名三级匹配~~ | ~~精确实体链接，减少重复节点~~ — **已在 V1.2 查询管道中实施为 `EntityResolver`**（精确匹配 → CONTAINS → difflib 模糊匹配，阈值 0.6）。详见 [V1.2 查询管道优化方案文档](V1.2查询管道优化方案文档.md)。 |
| P2 | 冲突检测增强（3个TODO） | checker.py 事实矛盾/过期内容/隐式实体 |
| P2 | 响应缓存层 | LLMResponseCache 减少重复 token 消耗 |
| P2 | 成本感知路由 | 按复杂度动态选择模型 |
| P2 | 微调数据准备 | 积累标注数据用于 SFT |

---

## 四、文件变更索引

### 修改文件（7个）
| 文件 | 变更行数 | 说明 |
|------|----------|------|
| `kb/app/config.py` | +5 / -5 | 模型默认值变更 + 注释 |
| `kb/app/models.py` | +2 | AnalysisReport 新增字段 |
| `kb/app/llm.py` | +69 / -3 | ResilientLLMClient + 工厂更新 |
| `kb/app/ingest/analyze.py` | +127 / -47 | Prompt 重写 + 动态上限 + few-shot |
| `kb/app/ingest/graph_process.py` | +169 / -44 | Prompt 重写 + 2-hop + 验证 |
| `kb/.env.example` | +7 / -2 | 分环节模型说明 |
| `kb/tests/test_feishu.py` | +2 / -1 | 动态上限测试适配 |

### 新增文件（3个）
| 文件 | 说明 |
|------|------|
| `kb/config/prompts/analyze_fewshot.txt` | 分类 few-shot（3示例，2108 chars） |
| `kb/config/prompts/implicit_fewshot.txt` | 隐式推理 few-shot（5示例，1499 chars） |
| `kb/app/ingest/fewshot.py` | Few-shot 加载器（46行） |

---

## 五、验证结果

```
✅ Python 编译检查：6/6 文件通过
✅ pytest 全量测试：176 通过（13.40s）
✅ Few-shot 加载器：analyze=2108 chars, implicit=1499 chars
✅ 动态上限测试：已适配新逻辑（content_length=500 → cap=10）
```

---

## 六、成本影响

> **⚠️ V2.0 迁移后更新**：以下为 V1.1 时代的 DashScope 定价。DeepSeek 迁移后的成本详见 [DeepSeek 迁移方案文档](DeepSeek迁移方案文档.md)。

| 项目 | V1.0 | V1.1 | V2.0 迁移后 | 变化 |
|------|------|------|-----------|------|
| 隐式推理模型 | ~~qwen-flash ($0.002/1K output)~~ | ~~qwen3.5-plus ($0.008/1K output)~~ | **deepseek-v4-pro** | 总体成本下降 |
| 单次 Ingest 推理成本 | ~$0.002 | ~$0.008 | ~$0.003（DeepSeek 更经济） | -62% vs V1.1 |
| 月成本（300次 Ingest） | ~$3 | ~$5 | ~$2.7（含查询） | V2.0 全链路成本 |
| Few-shot token 开销 | 0 | ~500 tokens/次 | ~500 tokens/次 | 可忽略 |
| Fallback 链开销 | N/A | 仅故障时触发 | 仅故障时触发 | 基本为0 |

**结论**：V1.1 月成本从 ~$3 增至 ~$5，换取推理质量 30-40% 提升。V2.0 迁移到 DeepSeek 后，全链路月成本进一步降至 ~$2.7（含 Ingest + Query），性价比进一步提升。

---

## 七、后续继承说明

V1.1 的优化成果已被后续迭代完整继承和扩展：

| V1.1 优化项 | V1.2 继承/演进 | V2.0 继承/演进 |
|------------|-------------|-------------|
| 动态实体上限 | ✅ 保留 | ✅ 保留 |
| 决策树分类 | ✅ 集成到 QueryUnderstander（jieba POS + 规则+LLM 混合分类） | ✅ 保留 |
| 2-hop 子图推理 | ✅ 集成到 GraphRetriever 的 4 策略 dispatch | ✅ 保留 + 多用户 user_id 隔离 |
| 推理验证（置信度过滤） | ✅ 演进为 QualityGate 质量门（SUFFICIENT/PARTIAL/INSUFFICIENT） | ✅ 保留 |
| Few-shot Learning | ✅ 保留 | ✅ 保留 |
| ResilientLLMClient 回退链 | ✅ 保留（新增 token 累计） | ✅ 演进：新增 `_DEEPSEEK_FALLBACKS` 回退链 |
| 8000 字符截断 | ✅ 保留 | ✅ 保留 |
| 结构化字段（aliases/subtype/domain） | ✅ 被 EntityResolver 用于精确/CONTAINS 匹配 | ✅ 保留 |

**相关文档**：
- [V1.2 查询管道优化方案文档](V1.2查询管道优化方案文档.md)
- [DeepSeek 迁移方案文档](DeepSeek迁移方案文档.md)
- [多用户认证与数据隔离方案文档](多用户认证与数据隔离方案文档.md)
